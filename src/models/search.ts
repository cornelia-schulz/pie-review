export interface ISearch {
  showMarker: boolean,
  showPopup: boolean,
  popupFormat: string,
  maxMarkers: number,
  retainZoomLevel: boolean,
  animateZoom: boolean
  autoClose: boolean,
  searchLabel: string,
  keepResult: boolean
}